version: 1
frontend:
  phases:
    build:
      commands:
        - cd frontend/
        - npm install
        - npm run build
  artifacts:
    baseDirectory: build
    files:
      - "**/*"
  cache:
    paths:
      - node_modules/**/*
